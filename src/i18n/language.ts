/**
 * A user's language preference. 'system' follows the device locale; 'ja' and
 * 'en' pin the app to a specific language regardless of the device.
 */
export type Language = 'system' | 'ja' | 'en';
